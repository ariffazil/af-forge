# Dependencies
node_modules/
*.log
*.pid
*.seed
*.pid.lock
.npm
.bun

# Build outputs
dist/
*.tsbuildinfo
*.js.map

# Environment / secrets
.env
.env.*
!.env.example
.secrets/
*.pem
*.key

# Runtime data
.agent-workbench/
.arifos/
*.jsonl
metrics/
!src/metrics/
!src/metrics/*.ts
scoreboard.json

# Backup files (any pattern)
*.bak*
*.backup*
memory.json

# Python / misc caches
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
*.egg-info/
.cache/
.mypy_cache/

# Test coverage
coverage/
.coverage
coverage.xml
htmlcov/

# IDEs
.vscode/
.idea/
*.swp
*.bak
*.tmp

# OS
.DS_Store
Thumbs.db

# Sibling projects (tracked via submodules or external means)
# !arifOS/
# !GEOX/

control_plane/
execution_plane/
ARCHIVE_*/
.archive/

# Agent / tool caches
.aider/
.aider.tags.cache.v4/
.claude/
.codex/
.copilot/
.gemini/
.kimi/
.mcp-auth/
.openclaw/
.opencode/
.playwright-mcp/

# Infrastructure secrets
.acme.sh/
.ssh/
.pki/
.docker/
.config/
.local/
.secrets/

# Misc archives
*.tar.gz
*.zip
*.tgz

.supabase/

# VAULT999 — runtime receipt cache. Supabase is primary ledger.
# Resolved: 2026-04-19 — F1 Amanah compliance
VAULT999/
.coverage
htmlcov/
coverage.xml
*.cover
data/amanah_locks.jsonl
coverage/

# ── RSI / Forge ephemeral artifacts — regenerated on every entropy pass ──
# The measure_entropy.py script writes this. It is derived data, not source.
entropy-report.json

# ── TypeScript build outputs ──
build/

# Secrets — never commit
.env
.env.local
.env.*.local

# Python cache & scratch
*.pyc
scratch/


# GitHub Copilot workspace — not VPS-aware, do not commit
Copilot/

# forge_work — session receipts, working notes, local artifacts
forge_work/
.audit_output.txt

# local ARA logs
duties/logs/
.registry/fingerprints.json
tools_sot.yaml
tools_sot.yaml
tools_sot.yaml
.megamemory/
flame/flame_config.json

# AForgePublish generated renders — source and tests stay tracked.
python/aforge_publish/artifacts/*.pdf
python/aforge_publish/artifacts/*.receipt.json
python/aforge_publish/artifacts/*.validator.json
python/aforge_publish/artifacts/*.typ
python/aforge_publish/artifacts/figures_*/*.png
CONTRIBUTING.md
.runtime/
deploy/rollback/
.serena/
.git_commit

# F1 AMANAH: operational snapshot backups (not source)
*.snap-*
.aider.chat.history.md
_backups/
vendor/

# === test output / regenerable (2026-09-11 BIJAKSANA sweep) ===

# === test output / regenerable (2026-09-11 BIJAKSANA sweep) ===
tests/mcp-conformance/results/conformance-*.json
artifacts/alpha-zen-daily/
