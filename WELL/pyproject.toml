[project]
name = "afwell"
version = "0.1.0"
description = "WELL — Biological Substrate Connector for arifOS"
dependencies = [
    "fastmcp>=2.12.3",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
